using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Load : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //OnScore.score = end.temp;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
